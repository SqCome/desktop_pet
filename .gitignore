node_modules/
dist/
*.log
.DS_Store
out/
release/

# Reminders service: SQLite files (db + journal + WAL) and any
# out-of-band build output. `dist/` already covers dist/server/ but
# these catch files that might end up in a developer's CWD by accident.
*.db
*.db-journal
*.db-wal
*.db-shm
server/bin/
server/coverage.out

# Claude Code local superpowers (skill state, plans) — local-only, never
# shared. The repo only keeps docs/superpowers/plans/* (already tracked).
.superpowers/

# Debug / diagnostic helpers — scripts the developer wrote to investigate
# a single machine's bridge install and never meant to keep around.
scripts/fix-hooks.py
scripts/patch-bridge.py